{
  "name": "app-waypoint",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "validate": "node scripts/validate-content.mjs",
    "build": "npm run validate && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.13.0",
    "yaml": "^2.8.1"
  }
}
