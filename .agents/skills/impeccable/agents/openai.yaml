interface:
  display_name: "Impeccable"
  short_description: "Design and review frontend interfaces"
  default_prompt: "Use $impeccable to improve this frontend within its existing design system."
policy:
  allow_implicit_invocation: true
